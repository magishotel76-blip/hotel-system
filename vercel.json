{
  "version": 2,
  "public": true,
  "buildCommand": "npx prisma generate && npm install --prefix frontend && npm run build --prefix frontend && rm -rf public && mkdir public && cp -r frontend/dist/. public/",
  "outputDirectory": "public",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.js" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "functions": {
    "api/*.js": {
      "includeFiles": "backend_logic/**"
    }
  }
}

{
  "name": "hotel-system-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "install-backend": "cd backend && npm install",
    "vercel-build": "npm run build && npm run install-backend"
  }
}
